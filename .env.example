# ── Required ──────────────────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxx
JWT_SECRET=change-me-to-64-char-random-string-here-use-openssl-rand
JWT_REFRESH_SECRET=another-64-char-random-string-for-refresh-tokens-use-openssl

# ── Database (leave blank for SQLite) ─────────────────────
# DATABASE_URL=postgresql://user:pass@host:5432/dbname

# ── Cache (leave blank for in-memory) ─────────────────────
# REDIS_URL=redis://user:pass@host:6379

# ── Server ────────────────────────────────────────────────
PORT=3000
NODE_ENV=development
LOG_LEVEL=info
